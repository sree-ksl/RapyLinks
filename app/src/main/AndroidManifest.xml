<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ksl.payrapyd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PayRapyd">
        <activity android:name=".HomeActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddProduct"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ShowProducts"/>
        <activity android:name=".CreateLinkActivity"/>
        <activity android:name=".TransactionsActivity"/>
        <activity android:name=".WalletInfo"/>
        <activity android:name=".CreateWalletActivity"/>
    </application>

</manifest>